"""Application configuration for the at home application."""

from django.apps import AppConfig


class AtHomeConfig(AppConfig):
    """Configuration object for the at home application."""

    name = "at_home"
