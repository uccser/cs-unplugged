"""Application configuration for the topics application."""

from django.apps import AppConfig


class TopicsConfig(AppConfig):
    """Configuration object for the topics application."""

    name = 'topics'
