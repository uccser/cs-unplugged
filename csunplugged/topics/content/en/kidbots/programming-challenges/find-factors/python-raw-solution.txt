factors = []
number = input('What\'s the number you want to find factors of? ')
divisor = 1
while divisor <= number:
  if number % divisor == 0:
    factors = factors + [divisor]
  divisor = divisor + 1
print(factors)