For the input `BWBBW` the code will output:

```text
Please enter cards (B for black and W for white) as one line, no spaces
9
```

For the input `WB` the code will output:

```text
Please enter cards (B for black and W for white) as one line, no spaces
2
```