"""Application configuration for the classic application."""

from django.apps import AppConfig


class ClassicConfig(AppConfig):
    """Configuration object for the classic application."""

    name = "classic"
