"""Application configuration for the resources application."""

from django.apps import AppConfig


class ResourcesConfig(AppConfig):
    """Configuration object for the resources application."""

    name = "resources"
